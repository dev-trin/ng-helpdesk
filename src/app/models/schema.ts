export class User {
    _id: string;
    username: string;
    password: string;
    firstName: string;
    lastName: string;
    email:string;
}

export class Group {
	_id: string;
	group: string;
	department: string;
}